class EmailService:

    @staticmethod
    def send_email(to: str, subject: str, body: str):
        # Stub for hackathon (replace with SMTP later)
        return {
            "status": "SENT",
            "to": to
        }
